import { getDisplayDate } from '../getDisplayDate';

describe('getDisplayDate', () => {
    it('returns date with timestamp when passed isoDate and timeStamp true', () => {
        expect('Hello').toEqual('Hello')
    })
})
